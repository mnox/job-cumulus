import Typography from '@mui/material/Typography';

export default function ReportsPage() {
  return (
    <Typography>I've heard reports that Matt is a wizard of data visualization when he has more than six minutes to work on it.</Typography>
  );
}